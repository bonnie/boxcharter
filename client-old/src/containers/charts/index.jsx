import React, {Component} from 'react';
import { ButtonToolbar, DropdownButton, MenuItem } from 'react-bootstrap';

export default class Charts extends Component {
  render() {
    return (
      <div>
       List charts here. Probably some snazzy search box too.
      </div>
    );
  }
}