import { renderComponent, expect } from '../test_helper';
import UserCharts from '../../src/components/user_charts';

describe('UserCharts', () => {
  beforeEach(() => {
    component = renderComponent(UserCharts);
  });
  
  it('', () => {
    
  });
});